[project]
name = "transcript-generator"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = ["torch>=2.5.1", "transformers>=4.46.2", "accelerate>=0.26.0"]
